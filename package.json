{
  "name": "foundry-story-deck",
  "version": "0.1.0",
  "description": "Campaign Story Decks — a JSON-driven collaborative Session 0 story-card engine for FoundryVTT.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "watch": "vite build --watch",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
